const $=s=>document.querySelector(s), $$=s=>document.querySelectorAll(s);function openMenu(){$('#side').classList.add('open');$('#overlay').classList.add('show')}function closeMenu(){$('#side').classList.remove('open');$('#overlay').classList.remove('show')}function show(id){$$('.page').forEach(x=>x.classList.remove('active'));$('#'+id).classList.add('active');closeMenu();if(id==='crypto')loadMarkets()}$('#menuBtn').onclick=openMenu;$('#close').onclick=closeMenu;$('#overlay').onclick=closeMenu;$$('[data-page]').forEach(x=>x.onclick=()=>show(x.dataset.page));const esc=x=>String(x).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));let markets=[];async function loadMarkets(){try{let r=await fetch('/api/binance/markets');markets=await r.json();$('#count').textContent=markets.length+' زوج USDT';render(markets.slice(0,300));$('#status').textContent='🟢 Binance متصل'}catch(e){$('#coins').innerHTML='<div class="empty">تعذر الاتصال بـ Binance</div>';$('#status').textContent='🔴 تعذر الاتصال'}}function render(a){$('#coins').innerHTML=a.map(x=>`<div class="coin" onclick="openAnalysis('${esc(x.symbol)}')"><b>${esc(x.symbol)}</b><div>${Number(x.price).toLocaleString()}</div><small>24س: ${Number(x.change).toFixed(2)}% — حجم: ${Number(x.quoteVolume).toLocaleString()}</small></div>`).join('')}$('#search').oninput=e=>{let q=e.target.value.toUpperCase();render(markets.filter(x=>x.symbol.includes(q)).slice(0,300))};function openAnalysis(s){$('#symbol').value=s;show('analysis');analyze()}$('#analyze').onclick=analyze;async function analyze(){let s=$('#symbol').value.toUpperCase(),i=$('#interval').value;$('#result').innerHTML='<div>جاري التحليل...</div>';try{let r=await fetch(`/api/binance/analysis?symbol=${encodeURIComponent(s)}&interval=${i}`),d=await r.json();if(!r.ok)throw Error(d.error);let c=d.signal.includes('شراء')?'green':d.signal.includes('بيع')?'red':'';$('#result').innerHTML=`<div><h3>الإشارة</h3><span class="signal ${c}">${d.signal}</span></div><div><h3>السعر</h3>${d.price}</div><div><h3>التغير</h3><span class="${d.change>=0?'green':'red'}">${d.change.toFixed(2)}%</span></div><div><h3>RSI</h3>${d.rsi.toFixed(2)}</div><div><h3>EMA 20</h3>${d.ema20}</div><div><h3>EMA 50</h3>${d.ema50}</div><div><h3>EMA 200</h3>${d.ema200}</div>`}catch(e){$('#result').innerHTML='<div class="empty">خطأ: '+esc(e.message)+'</div>'}}loadMarkets();

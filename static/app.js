(()=>{"use strict";const $=id=>document.getElementById(id);const esc=x=>String(x??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]));const n=x=>{const v=Number(x);return Number.isFinite(v)?v.toLocaleString("en-US",{maximumFractionDigits:8}):"-"};const cl=s=>String(s||"").includes("شراء")?"buy":String(s||"").includes("بيع")?"sell":"neutral";function card(r,type){const fut=type!=="سبوت";const dir=r.direction==="شراء"?"LONG":"SHORT";return '<article class="trade-card"><div class="trade-top"><div><div class="symbol">'+esc(r.symbol||"-")+'</div><div class="tag">'+esc(type)+' · '+esc(r.interval||"-")+' · ● AI LIVE</div></div><div class="signal '+cl(r.signal)+'">'+esc(fut?dir:r.signal||"حيادي")+'</div></div><div class="trade-price"><small>السعر / الدخول</small><b>'+n(r.entry||r.price)+'</b></div><div class="levels"><div class="level"><small>TP1</small><b>'+n(r.tp1)+'</b></div><div class="level tp"><small>TP2</small><b>'+n(r.tp2)+'</b></div><div class="level tp"><small>TP3</small><b>'+n(r.tp3)+'</b></div><div class="level sl"><small>SL</small><b>'+n(r.sl)+'</b></div></div><div class="meta"><span>🤖 ثقة '+n(r.confidence)+'%</span><span>⚖️ R:R '+n(r.rr)+'</span>'+(fut?'<span>⚡ رافعة '+esc(r.leverage||"محسوبة")+'</span>':"")+'</div></article>'}async function get(u){const r=await fetch(u,{cache:"no-store"});if(!r.ok)throw Error(r.status);return r.json()}async function load(kind){const body=$(kind+"Body"),st=$(kind+"Status");if(!body)return;st.textContent="🤖 يجمع البيانات ثم يحللها...";try{const market=kind==="spot"?"crypto":"futures";const d=await get("/api/ai/signals?market="+market+"&interval=15m&limit=20");let rows=(d.results||[]).filter(x=>x.tradeReady);body.innerHTML=rows.length?rows.map(x=>card(x,kind==="spot"?"سبوت":kind==="futures"?"فيوتشر":"عقود آجلة")).join(""):'<div class="empty">ما فيه فرصة قوية حاليًا — AI ينتظر إعدادًا أفضل.</div>';st.textContent="آخر تحليل "+new Date().toLocaleTimeString("ar-SA")+" · "+rows.length+" صفقة"}catch(e){body.innerHTML='<div class="empty">تعذر التحليل حاليًا.</div>';st.textContent="تعذر الاتصال بمصدر البيانات"}}async function home(){const b=$("homeTrades");if(!b)return;try{const a=await get("/api/ai/signals?market=crypto&interval=15m&limit=12"),f=await get("/api/ai/signals?market=futures&interval=15m&limit=12");const rows=[...(a.results||[]).map(x=>[x,"سبوت"]),...(f.results||[]).map(x=>[x,"فيوتشر"])].filter(x=>x[0].tradeReady).sort((x,y)=>(y[0].confidence||0)-(x[0].confidence||0)).slice(0,9);b.innerHTML=rows.length?rows.map(x=>card(x[0],x[1])).join(""):'<div class="empty">AI ينتظر فرصًا تستوفي الفلترة.</div>'}catch(e){b.innerHTML='<div class="empty">جاري إعادة المحاولة...</div>'}}document.addEventListener("DOMContentLoaded",async()=>{const p=document.body.dataset.page;if(p==="spot"){load("spot");$("refreshSpot")?.addEventListener("click",()=>load("spot"));setInterval(()=>load("spot"),60000)}if(p==="futures"){load("futures");$("refreshFutures")?.addEventListener("click",()=>load("futures"));setInterval(()=>load("futures"),60000)}if(p==="contracts"){load("contracts");$("refreshContracts")?.addEventListener("click",()=>load("contracts"));setInterval(()=>load("contracts"),60000)}if(p==="dashboard")home();$("menuBtn")?.addEventListener("click",()=>$("sidebar")?.classList.toggle("open"));try{const m=await get("/api/auth/me");if($("userBadge"))$("userBadge").textContent=m.user?"👤 "+m.user:""}catch(e){}})})();